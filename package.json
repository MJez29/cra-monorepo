{
  "name": "cra-monorepo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:a": "cross-env REACT_APP_SITE=a npm run start",
    "start:b": "cross-env REACT_APP_SITE=b npm run start",
    "build": "react-scripts build",
    "build:a": "cross-env REACT_APP_SITE=a npm run build",
    "build:b": "cross-env REACT_APP_SITE=b npm run build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
